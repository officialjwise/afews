# A-FEWS Frontend — local development environment
# DO NOT commit secrets to version control.

# Backend API base URL (no trailing slash, no /v1 suffix needed — it's in api.ts)
VITE_API_BASE=http://localhost:8000/v1
